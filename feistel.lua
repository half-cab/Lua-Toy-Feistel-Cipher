--[[
Toy-Feistel-Cipher-in-Lua
MIT License (c) 2025 Cleary Bettisworth
https://github.com/half-cab/Toy-Feistel-Cipher-in-Lua
]]

-- feistel.lua
-- Created by Cleary Bettisworth
-- Simple Feistel style cipher implementation

local feistel = {}

local xor, padInput, unpadInput
math.randomseed(os.time())

local sbox = {
    
}

-- XOR helper function
function xor(a, b)
    local result = ""
    for i = 1, math.min(#a, #b) do
        local byteA = string.byte(a, i)
        local byteB = string.byte(b, i)
        local xorByte = byteA ~ byteB
        result = result .. string.char(xorByte)
    end
    return result
end

-- Add PKCS#7 style padding
function padInput(input, blockSize)
    local padLen = blockSize - (#input % blockSize)
    local padding = string.rep(string.char(padLen), padLen)
    return input .. padding
end

-- Remove PKCS#7 style padding
function unpadInput(input)
    local padLen = string.byte(input, -1)
    return string.sub(input, 1, #input - padLen)
end

-- Random key generator
function feistel.genRandKey(length)
    local charset = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890"
    local key = ""
    for i = 1, length do
        local num = math.random(#charset)
        local char = string.sub(charset, num, num)
        key = key .. char
    end
    return key
end

-- Split the key into subkeys
function feistel.genSubKey(key, rounds)
    local subkeys = {}
    for i = 1, rounds do
        local rotated = key:sub(i) .. key:sub(1, i-1)
        subkeys[i] = xor(rotated, string.rep(string.char(i), #key))
    end
    return subkeys
end

-- Feistel round function
function feistel.roundFunction(data, subkey)
    local mixed = xor(data, subkey)
    local result = ""
    for i = 1, #mixed do
        local byte = string.byte(mixed, i)
        result = result .. string.char(sbox[byte + 1])
    end
    return result
end

-- Split input into two halves
function feistel.splitInput(input)
    local size = math.floor(#input / 2)
    local leftSide = string.sub(input, 1, size)
    local rightSide = string.sub(input, size + 1)
    return {leftSide, rightSide}
end

-- Encrypt a single block
function feistel.encryptBlock(block, key, rounds)
    local halves = feistel.splitInput(block)
    local subkeys = feistel.genSubKey(key, rounds)
    local left = halves[1]
    local right = halves[2]

    for i = 1, rounds do
        local newRight = xor(left, feistel.roundFunction(right, subkeys[i]))
        left, right = right, newRight
    end

    return left .. right
end

-- Decrypt a single block
function feistel.decryptBlock(block, key, rounds)
    local halves = feistel.splitInput(block)
    local subkeys = feistel.genSubKey(key, rounds)
    local left = halves[1]
    local right = halves[2]

    for i = rounds, 1, -1 do
        local newLeft = xor(right, feistel.roundFunction(left, subkeys[i]))
        right, left = left, newLeft
    end

    return left .. right
end

-- Encrypt full input string
function feistel.encrypt(input, key, rounds, blockSize)
    input = padInput(input, blockSize)
    local ciphertext = ""

    for i = 1, #input, blockSize do
        local block = string.sub(input, i, i + blockSize - 1)
        ciphertext = ciphertext .. feistel.encryptBlock(block, key, rounds)
    end

    return ciphertext
end

-- Decrypt full ciphertext string
function feistel.decrypt(ciphertext, key, rounds, blockSize)
    local plaintext = ""

    for i = 1, #ciphertext, blockSize do
        local block = string.sub(ciphertext, i, i + blockSize - 1)
        plaintext = plaintext .. feistel.decryptBlock(block, key, rounds)
    end

    return unpadInput(plaintext)
end

return feistel
